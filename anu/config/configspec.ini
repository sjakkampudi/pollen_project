class_size = integer(min=1, default=10000)

train_size = float(min=0.01, default=0.65)
secret_size = float(min=0.01, default=0.05)

epochs = integer(min=1, default=2)
conv_layers = integer(min=1, default=4)
num_filters = integer(min=2, default=32)
square_filter_size = integer(min=1, default=3)
pool_size = integer(min=1, default=2)
dense_layer_dim = integer(min=1, default=168)
seed_value = integer(min=0, default=1)

balance_classes = boolean(default=False)